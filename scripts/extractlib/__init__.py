# -*- coding: utf-8 -*-
"""`gw extract` 의 부품들.

extract.py 는 오케스트레이션만 하고, 실제로 PDF 와 씨름하는 코드는 전부 여기 있다.
모듈이 나뉘어 있는 이유는 실패 지점이 서로 다르기 때문이다.

  textnorm  글자 수준 (PUA 글리프, NFKC, 깨진 인코딩)
  sources   파일 수준 (어떤 파일이 문제지/정답/해설인가, 텍스트 레이어가 있는가)
  answers   정답 수준 (세 축 교차 검증)
  layouts   판형 수준 (문항 분리·발문/보기/선택지 파싱 전략 선택)
  tamgu     탐구 영역 판형 구현
"""
