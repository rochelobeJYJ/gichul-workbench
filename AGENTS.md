# gichul-workbench

한국 수능·모의평가 기출을 내려받아 문항별 크롭·전사·단원분류하고 인쇄용 문항집을 만드는 도구.
사회탐구·과학탐구 17과목.

**`SKILL.md` 를 읽어라. 그것이 이 저장소의 사용 설명이다.**
여기 있는 것은 Codex 에서만 다른 점이다.

## Codex 에서 주의할 것

- PowerShell 에서는 `&&` 가 없다. `A; if ($?) { B }` 로 쓴다.
- 경로에 한글이 있으므로 인용부호를 빠뜨리지 마라.
- 파이썬은 시스템 파이썬을 쓴다. 가상환경이 필요하면 사용자에게 물어라.
- 파일을 쓸 때 인코딩은 UTF-8 로 명시한다. `Set-Content` 기본값은 ANSI 다.

## 절대 하지 말 것

- `workspace/` 안의 파일을 통째로 읽어 컨텍스트에 올리는 것. 380개짜리 폴더다.
  각 명령이 남기는 `reports/<step>.json` 만 읽는다.
- PDF 를 직접 파싱하려 드는 것. 스크립트가 한다.
- 저작물(문제지·해설 PDF, 크롭 이미지)을 커밋하거나 외부로 보내는 것.
